DOWOD 13.08 10:00:44 CEST — punkty, ktore Zenek oznaczyl BRAK SLADU (jego sandbox nie ma dostepu do magistrali systemd)
--- systemctl is-active hans-ucho.service:
active
--- systemctl is-active hans-ucho-watchdog.timer:
active
--- proces:
1280764 /usr/bin/python3 /root/rod-ai-studio/tools/hans_ucho.py --petla 60
1282026 bash -lc cd /root/rod-ai-studio && { echo "DOWOD 13.08 $(TZ=Europe/Warsaw date '+%H:%M:%S %Z') — punkty, ktore Zenek oznaczyl BRAK SLADU (jego sandbox nie ma dostepu do magistrali systemd)"; echo "--- systemctl is-active hans-ucho.service:"; systemctl is-active hans-ucho.service; echo "--- systemctl is-active hans-ucho-watchdog.timer:"; systemctl is-active hans-ucho-watchdog.timer; echo "--- proces:"; pgrep -af "tools/hans_ucho.py"; echo "--- getMe bota (czy token zyje, bez getUpdates):"; python3 - <<'PY' import sys, requests sys.path.insert(0,"tools") from pakiet_wznowienia import _token_hansa t,c = _token_hansa() r = requests.get(f"https://api.telegram.org/bot{t}/getMe", timeout=20).json() print("ok=", r.get("ok"), "bot=", r.get("result",{}).get("username"), "chat_id ustawiony=", bool(c)) PY echo "--- ponowna wysylka pakietu (pelna odpowiedz Telegrama):"; python3 - <<'PY' import sys, json sys.path.insert(0,"tools") import pakiet_wznowienia as pw, requests t,c = pw._token_hansa() with open(".scratch/pakiet/WZNOWIENIE.txt","rb") as f:     r = requests.post(f"https://api.telegram.org/bot{t}/sendDocument",         data={"chat_id": c, "caption":"TEST KONCOWY — pakiet wznowienia dziala. Wklejaj ten plik do nowego okna po blokadzie."},         files={"document":("WZNOWIENIE.txt", f, "text/plain")}, timeout=60).json() res = r.get("result",{}) print("ok=", r.get("ok"), "| message_id=", res.get("message_id"), "| plik=", res.get("document",{}).get("file_name"), "| rozmiar=", res.get("document",{}).get("file_size"), "| data=", res.get("date")) PY } 2>&1 | tee .scratch/pakiet/dowod_zenek_brak_sladu.txt
1282029 bash -lc cd /root/rod-ai-studio && { echo "DOWOD 13.08 $(TZ=Europe/Warsaw date '+%H:%M:%S %Z') — punkty, ktore Zenek oznaczyl BRAK SLADU (jego sandbox nie ma dostepu do magistrali systemd)"; echo "--- systemctl is-active hans-ucho.service:"; systemctl is-active hans-ucho.service; echo "--- systemctl is-active hans-ucho-watchdog.timer:"; systemctl is-active hans-ucho-watchdog.timer; echo "--- proces:"; pgrep -af "tools/hans_ucho.py"; echo "--- getMe bota (czy token zyje, bez getUpdates):"; python3 - <<'PY' import sys, requests sys.path.insert(0,"tools") from pakiet_wznowienia import _token_hansa t,c = _token_hansa() r = requests.get(f"https://api.telegram.org/bot{t}/getMe", timeout=20).json() print("ok=", r.get("ok"), "bot=", r.get("result",{}).get("username"), "chat_id ustawiony=", bool(c)) PY echo "--- ponowna wysylka pakietu (pelna odpowiedz Telegrama):"; python3 - <<'PY' import sys, json sys.path.insert(0,"tools") import pakiet_wznowienia as pw, requests t,c = pw._token_hansa() with open(".scratch/pakiet/WZNOWIENIE.txt","rb") as f:     r = requests.post(f"https://api.telegram.org/bot{t}/sendDocument",         data={"chat_id": c, "caption":"TEST KONCOWY — pakiet wznowienia dziala. Wklejaj ten plik do nowego okna po blokadzie."},         files={"document":("WZNOWIENIE.txt", f, "text/plain")}, timeout=60).json() res = r.get("result",{}) print("ok=", r.get("ok"), "| message_id=", res.get("message_id"), "| plik=", res.get("document",{}).get("file_name"), "| rozmiar=", res.get("document",{}).get("file_size"), "| data=", res.get("date")) PY } 2>&1 | tee .scratch/pakiet/dowod_zenek_brak_sladu.txt
--- getMe bota (czy token zyje, bez getUpdates):
ok= True bot= HansFabrykaRolek_bot chat_id ustawiony= True
--- ponowna wysylka pakietu (pelna odpowiedz Telegrama):
ok= True | message_id= 53 | plik= WZNOWIENIE.txt | rozmiar= 12536 | data= 1786608045
