# Paperclip - Manage AI Agents Like a Company (Full Deep Dive)
# Full Stack, https://youtu.be/9o16uyJB0os
# zrodlo: droga domowa (yt-dlp na HA DOM), 18.08.2026, napisy EN

20 Claude code terminals, all solving
different problems, none talking to each
other. Reboot the machine and the entire
empire of context disappears. Every tab
is a fresh personality. None of them
remember yesterday. Meet Paperclip, the
open-source app everyone uses to manage
AI agents at work. It is orchestration
for a team of agents, and right now it
is exploding. Self-hosted, MIT licensed,
and taking the agent world by storm. If
Open Claude is an employee, Paperclip is
the company. It pulls your agents into
one organization with
managers, budgets, and real
accountability for every action. Every
hire reports somewhere, and every dollar
spent is tracked. Since March, this
TypeScript mono repo has blown past
76,000 stars, 14,000 forks, MIT
licensed, and that growth curve is still
climbing hard. Created in March, and by
August it is the default answer for
agent orchestration. Paperclip manages
business goals, not pull requests. You
declare a mission, hire your agents, set
the budgets, and watch the work get done
from one dashboard. No more babysitting
sessions, no more lost work between
tools. Real talk, how are you
coordinating agents today? A dozen
terminal tabs, context pasted by hand,
and no record of who did what, when, or
why it mattered. You are the memory, the
router, and the manager all at once.
When a loop goes sideways, it burns
tokens silently. Hundreds of dollars can
evaporate before you even notice the
runaway. Your quota dies, and the work
was garbage anyway, and there is no
dashboard telling you where it all went.
Every folder holds a different config.
Every agent runs its own little system.
You end up reinventing task management,
communication, and hiring from scratch
on top of everything else. Meanwhile,
the actual business never quite gets
built. This is not a to-do list problem.
It is a company problem. What you
actually need is a control plane for the
whole operation. One surface for every
agent, every task, and every dollar.
Paperclip is a Node.js server with a
React dashboard. It orchestrates a team
of AI agents, assigning goals, tracking
work, and controlling cost from one
place. The server holds the state. The
dashboard gives you the boardroom view.
A company is a first-class object. One
paperclip deployment can run many
companies side by side each with
complete data isolation and its own
audit trail. Each one gets its own goal,
employees, org structure, and revenue
tracking. Every task traces upward
through parents all the way to the
company mission. An agent can always
answer the question, "Why am I doing
this?" The chain runs from a task to a
project to a team goal to the mission.
It looks like a task manager. Under the
hood, it is an org chart, a budget
system, a governance layer, and a
coordination engine. One screen answers
it all: who is working, what it costs,
and whether it is working. Pillar one,
the agentic task manager. Declare
intent, agents work, you verify the
output. Review from diffs, screenshots,
and tests before anything ships.
Verification is built into the loop, not
bolted on after the fact. Pillar two, an
org chart for agents. Titles, reporting
lines, permissions, and scoped secrets.
Every agent has a boss, a job
description, and boundaries. Governance
decides who can do what down to the
scoped secrets. Every agent is defined
by its adapter config. A Claude code
agent reads its context file. An open
claw agent runs on sole and heartbeat
files. Paperclip does not prescribe the
format. The minimum contract is simply
this: be callable. Pillar three, agent
employee training. A skill studio shared
org-wide skills, evals, and performance
reviews that keep sharpening your AI
staff. Treat your agents like staff and
they improve like staff. Pillar four,
the agentic OS. Cross-provider runtime,
sandboxing MCP servers, SSO, RBAC, and
cost controls across the entire stack.
The infrastructure layer that makes
every other pillar reliable. Tasks, org,
training, infrastructure. Four pillars,
one control plane, built so a company of
agents actually produces. Each pillar is
a product surface and all of them share
one data model. Bring your own agent any
runtime. If it can receive a heartbeat,
it is hired. No vendor lock, no
migration project. No vendor lock, no
migration project, no rewriting your
agents. Open Claw, Clawed Code, Codex,
Cursor, Gemini, Plain Bash, or HTTP
bots. Adapters define how each heartbeat
is invoked, observed, and canceled. The
control plane does not run your agents,
it coordinates them. A dedicated gateway
integrates Open Claw style agents,
remote, always on, and fully observable
through the same org chart and audit
trail. Built for the remote, always on
agents that live outside your machine.
Agents wake on a schedule, check their
work, and act. Heartbeats are the pulse
that keeps a distributed workforce
alive, even while you sleep. Each beat
is a chance to check work, claim tasks,
and report cost. Agents also run on
event triggers, task assignment, at
mentions, webhooks. Continuous agents
like Open Claw stay awake and report
back with results. You decide the wake
signals, and Paperclip handles the rest.
Delegation flows up and down the org
chart. A CEO agent reviews the
executives, reprioritizes, and assigns
new strategic initiatives. The tree
keeps the loop tight, from mission down
to execution. Tasks carry full goal
ancestry. When an engineer picks up a
ticket, it knows the project, the team
goal, and the company mission behind it.
The why is never lost, even across
months of accumulated work. Execution is
atomic. A task checks out under a lock,
so two agents never do the same work
twice. No double work, no lost context.
Execution locks, blocker dependencies,
recovery issues, the boring details that
save you. Monthly budgets per agent,
tracked in tokens and dollars. When an
agent hits the limit, it stops. No
runaway spend, no surprise invoices.
Warning thresholds and hard stops work
per agent, so one runaway cannot sink
the company. Proof hires, review
strategy, pause or terminate any agent
at any time. Governance with rollback.
Bad changes get reverted safely.
Decision tracking keeps a permanent
record of every approval. Watchdogs
verify results. Recovery actions fix
failures. Review gates block bad work.
The loop closes on quality, not just
activity. Self-healing runs recover
orphaned work automatically. Every
conversation is threaded, every decision
explained, every tool call traced, an
immutable audit log you can actually
inspect. Issues carry comments,
documents, attachments, and work
products. Secrets live encrypted, scoped
per agent and per run. Sensitive values
stay out of prompts unless the work
explicitly needs them. Encrypted at
rest, injected only into scoped runs.
Routines handle the recurring work,
customer support, reports, social, cron,
webhook, and API triggers wake the right
agent automatically. Each run creates a
tracked issue and wakes the assigned
agent. Each task gets the right
directory, project workspaces, Git work
trees, dev servers, preview URLs. Agents
work in the right place every time. No
more agents working in the wrong folder.
Multi-company by default, one
deployment, many businesses, complete
isolation, a control plane for your
whole portfolio, teams, clients, or
experiments, each one fully sealed. The
dashboard is mobile ready. Check on your
autonomous businesses, approve work, and
chime in from anywhere. The board fits
in your pocket and the agents do not
rest. Plugin system extends Paperclip
without forking it, knowledge bases,
custom tracing, queues, out-of-process
workers with capability gated services.
Capability gated host services keep
every extension safe. Under the hood, 12
systems work together, identity, org
chart, tasks, heartbeats, workspaces,
governance, budgets, routines, plugins,
secrets, activity, portability.
Everything company scoped, everything
logged, everything traceable. Two
layers, the control plane coordinates
and execution services run the agents.
Agents live where they live and phone
home after every heartbeat. Adapters
connect Claude code, Codex, HTTP bots,
and everything in between. Identity and
access covers two deployment modes,
board users, agent API keys, and
short-lived run tokens. Every mutating
request traces to an actor. Board
operators get full control, agents get
scoped API keys. The work system is
ticket based. Issues carry company,
project, goal, and parent links with
blockers, comments, documents, and work
products attached. A single assignee per
issue keeps responsibility unambiguous.
Heartbeat execution runs on a database
backed wake-up queue with coalescing,
budget checks, workspace resolution, and
secret injection before every run.
Orphaned runs are recovered
automatically by the engine. Execution
policies add review and approval stages.
Nothing ships without your sign-off, and
every approval decision is recorded.
Pause, resume, terminate, all governed,
all logged. Costs are tracked by
company, agent, project, goal, provider,
and model. Warning thresholds alert you.
Hard stops pause agents automatically.
Overspend pauses agents and cancels
queued work automatically. Every
mutation, heartbeat state change, cost
event, and approval lands in a durable
activity log. You can audit exactly what
happened and why. A timeline of what
happened and who is responsible. Export
and import entire organizations, agents,
skills, projects, routines, issues with
secret scrubbing and collision handling.
Move a whole company between deployments
without a single secret. Open telemetry
tracing is opt-in on the server, and
anonymous usage telemetry can be
disabled with a single environment
variable. Traces only, no personal data,
and repo references stay hashed.
Paperclip is not a chatbot. Agents have
jobs, not chat windows. They wake up, do
the work, and report back. No open-ended
chat sessions to babysit. It is not an
agent framework. You bring your own
agents and their runtimes. Paperclip
runs the organization they work in.
Paperclip never tells you how to build
an agent. It is not a prompt manager,
either. Agents bring their own prompts,
models, and runtimes. Paperclip manages
the organization they work in. Your
prompts stay yours end-to-end. No
drag-and-drop pipelines. Paperclip
models companies, org charts, goals,
budgets, governance, not flows.
Structure over scripts. The organization
itself is the automation. And it is not
a single agent tool. With one agent, you
might not need it. With 20, you
definitely do. Teams of agents are the
point. A single bot is not a company. It
is not a code review tool, either.
Paperclip orchestrates work, not pull
requests. Bring your own review process.
Pull requests stay where they belong,
inside your own process. Agents are
everywhere, but nobody is running them
like a business. Paperclip is the
missing operating layer for the
autonomous workforce, the operating
system layer for work that never sleeps.
Not a wrapper, not a framework. It is a
full control plane, and it is open
source, self-hosted, MIT licensed, and
yours to run anywhere. To run it
yourself, you need Node.js version 20 or
newer, plus pnpm 9.15 or newer. That is
basically it. Both install quickly on
any modern machine. The one-line
installer pulls a checksum-verified
script and runs interactive onboarding.
It can even install Paperclip as a
background service. Checksums verify the
script before it touches your system.
Want to try it without installing
anything? npx paperclip-i onboard yes
boots the whole thing and walks you
through setup. No repo, no clone, no
config files to create. Or, clone the
repo, run pnpm install, then pnpm dev.
The API server comes up on port 3100.
The whole stack server, UI, CLI, runs
from one command. No database setup
required. An embedded SQLite database is
created automatically on first run.
Postgres compatible, zero config,
perfect for local development. Curl
localhost:3100/api/health,
and you are live. Add your agents,
define a goal, and start hiring. The
board is served right from the API in
dev mode. Create a company, define the
mission, then hire a CEO agent. Build
out the org tree, CTO, engineers,
marketers, each with their own adapter.
Every employee is an agent, and every
agent has a role. Review strategy, set
budgets, hit go. Heartbeat start firing,
tasks get checked out, and work shows up
in the dashboard in real time. The
dashboard becomes the boardroom for your
company. Atomic execution, persistent
agent state, runtime skill injection,
governance with rollback. The hard
orchestration details are handled for
you. The orchestration details are
handled correctly, so you do not have
to. Memory work queues, CEO chat,
connected apps, the roadmap keeps
shipping. And with an MIT license, the
future stays open. Cloud deployments and
connected apps are on deck next. The
goal is simple, make autonomous
companies capable, governable, and real.
Paperclip is the backbone they run on.
Thousands of companies, one shared
operating layer. If this is the
direction you want your agent workflow
to go, star the repo on GitHub. It is
the fastest way to support the project.
It helps the project, and it tells the
algorithm you care. Install it today.
Open source, self-hosted, no account
required. Your agents have a company to
run. The install is one command, and the
first run takes minutes. Like this video
if it helped. Drop your agent management
horror stories in the comments.
Subscribe for more deep dives, and star
the repo. The next deep dive is already
in the works.